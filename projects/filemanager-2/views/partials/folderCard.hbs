<a href="/filemanager/{{folder.relativePath}}">
  <form class="folder-card__wrapper" onsubmit="return confirm('czy usunąć?')">
    <div class="folder-card__content d-flex align-items-center gap-3">
      <i class="bi bi-folder"></i>
      <i class="bi bi-folder2-open"></i>
      <h6 class="m-0">{{clipFileName folder.name}}</h6>
    </div>
    <button formaction="/delete/{{folder.relativePath}}"><i
        class="bi bi-x"
      ></i></button>
  </form>
</a>